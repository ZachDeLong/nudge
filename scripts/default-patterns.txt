# Nudge — opt-in patterns. Edit and save; the hook re-reads on every call.
# Lines starting with # are ignored. Full syntax: docs/patterns.md.

Bash(git push:*)
Bash(rm:*)
Bash(*--force*)
Bash(*deploy*)
Edit(/etc/**)
Write(/etc/**)
Edit(**/.env*)
Write(**/.env*)
